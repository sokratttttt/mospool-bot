# 🚂 Деплой на Railway

## Шаг 1: Создайте GitHub репозиторий

1. Откройте https://github.com/new
2. Название: `mospool-bot`
3. Приватный репозиторий
4. Нажмите "Create repository"

## Шаг 2: Загрузите код на GitHub

Откройте командную строку в папке `bot`:

```cmd
cd "D:\mos-pool channel\bot"

git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ВАШ_ЛОГИН/mospool-bot.git
git push -u origin main
```

## Шаг 3: Создайте проект на Railway

1. Откройте https://railway.app/
2. Нажмите "Start a New Project"
3. Выберите "Deploy from GitHub repo"
4. Авторизуйтесь через GitHub
5. Выберите репозиторий `mospool-bot`

## Шаг 4: Настройте переменные окружения

В Railway dashboard:
1. Откройте ваш проект
2. Нажмите на сервис (фиолетовый блок)
3. Перейдите во вкладку "Variables"
4. Добавьте переменные (нажмите "New Variable"):

```
TELEGRAM_BOT_TOKEN = ваш-токен-бота
TELEGRAM_CHANNEL_ID = @mospool
ADMIN_TELEGRAM_ID = ваш-telegram-id
MISTRAL_API_KEY = ваш-ключ-mistral
VK_ACCESS_TOKEN = ваш-токен-vk (если нужен)
VK_GROUP_ID = id-группы-vk (если нужен)
```

## Шаг 5: Деплой!

Railway автоматически задеплоит при добавлении переменных.

Проверьте логи:
1. Откройте проект
2. Нажмите на сервис
3. Вкладка "Logs"

Должно появиться:
```
✅ Database initialized
🚀 Bot starting...
```

## ✅ Готово!

Ваш бот теперь работает 24/7 на Railway!

---

## 🔧 Обновление бота

Когда захотите обновить код:

```cmd
git add .
git commit -m "Update"
git push
```

Railway автоматически задеплоит новую версию.

---

## 💡 Советы

- **Логи:** Railway → ваш проект → Logs
- **Перезапуск:** Settings → Restart
- **Лимиты:** 500 часов/месяц бесплатно
- **Мониторинг:** Metrics показывает CPU/RAM

---

## ⚠️ Важно

Файл `.env` НЕ загружайте на GitHub!
Переменные добавляйте только через Railway Dashboard.

Добавьте `.env` в `.gitignore`:
```
.env
data/
__pycache__/
*.pyc
venv/
```
