# 🏊 Система автоматизации публикаций для бассейновой компании

Полностью **бесплатная** система для автоматизации постинга в Telegram и VK/Max.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте его (Windows)
venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка окружения

```bash
# Скопируйте пример конфига
copy .env.example .env

# Отредактируйте .env файл, добавив свои токены
```

### 3. Инициализация базы данных

```bash
# Создайте папку для логов
mkdir logs

# Выполните миграции
python manage.py makemigrations posts
python manage.py migrate

# Создайте суперпользователя
python manage.py createsuperuser
```

### 4. Загрузите начальные данные

```bash
python manage.py shell
```

```python
from apps.posts.models import PostCategory, Platform

# Создаём категории
PostCategory.objects.get_or_create(slug='project', defaults={'name': '🏊 Новый проект'})
PostCategory.objects.get_or_create(slug='tip', defaults={'name': '💡 Полезный совет'})
PostCategory.objects.get_or_create(slug='promo', defaults={'name': '🎁 Акция/Скидка'})
PostCategory.objects.get_or_create(slug='case', defaults={'name': '📸 Кейс/Отзыв'})
PostCategory.objects.get_or_create(slug='edu', defaults={'name': '📚 Образовательный'})

# Создаём платформы (заполните токены потом в админке)
Platform.objects.get_or_create(
    name='telegram',
    defaults={
        'display_name': 'Telegram',
        'api_token': '',
        'channel_id': ''
    }
)
Platform.objects.get_or_create(
    name='vk',
    defaults={
        'display_name': 'VK / Max',
        'api_token': '',
        'channel_id': ''
    }
)

exit()
```

### 5. Запуск сервера

```bash
python manage.py runserver
```

Откройте http://127.0.0.1:8000/

---

## 📱 Настройка платформ

### Telegram

1. Напишите [@BotFather](https://t.me/BotFather) команду `/newbot`
2. Получите токен бота
3. Создайте канал и добавьте бота администратором
4. В .env или в админке укажите:
   - `TELEGRAM_BOT_TOKEN` - токен бота
   - `TELEGRAM_CHANNEL_ID` - @username канала или числовой ID

### VK / Max

1. Создайте приложение: https://vk.com/apps?act=manage
2. Тип: Standalone
3. Получите access_token через Implicit Flow:
   ```
   https://oauth.vk.com/authorize?client_id=ВАШ_APP_ID&scope=wall,photos,groups&response_type=token
   ```
4. В .env или в админке укажите:
   - `VK_ACCESS_TOKEN` - токен
   - `VK_GROUP_ID` - ID группы (только цифры)

### DeepSeek AI (бесплатно)

1. Зарегистрируйтесь: https://platform.deepseek.com/
2. Получите API ключ
3. В .env укажите:
   - `DEEPSEEK_API_KEY` - ваш ключ

---

## 🎯 Использование

### Веб-интерфейс

- **Дашборд** `/` — статистика и обзор
- **Посты** `/posts/` — список всех постов
- **Создать** `/posts/create/` — новый пост
- **Генерация** `/generate/` — создание контента с ИИ
- **Календарь** `/calendar/` — расписание публикаций
- **Проекты** `/projects/` — база проектов бассейнов
- **Настройки** `/settings/` — конфигурация системы
- **Django Admin** `/admin/` — расширенное управление

### Workflow

1. Добавьте проект бассейна (вручную или парсинг)
2. Сгенерируйте текст поста (ИИ или шаблон)
3. Отредактируйте и настройте
4. Выберите платформы и время
5. Одобрите пост
6. Система опубликует автоматически

---

## 📁 Структура проекта

```
├── manage.py
├── requirements.txt
├── .env                    # Ваши токены (не коммитить!)
│
├── config/                 # Django настройки
│
├── apps/
│   ├── posts/              # Управление постами
│   ├── publishers/         # Telegram/VK публикаторы
│   ├── scheduler/          # APScheduler планировщик
│   └── ai_generator/       # DeepSeek интеграция
│
├── templates/              # HTML шаблоны
└── static/                 # CSS, JS, изображения
```

---

## 💰 Стоимость

**0 рублей!**

- ✅ Telegram Bot API — бесплатно
- ✅ VK API — бесплатно
- ✅ DeepSeek AI — бесплатный tier
- ✅ SQLite — бесплатно
- ✅ Локальный хостинг — бесплатно

---

## 🔧 Запуск как сервис (Windows)

Создайте `start_server.bat`:

```batch
@echo off
cd /d "D:\mos-pool channel"
call venv\Scripts\activate
python manage.py runserver 0.0.0.0:8000
```

Добавьте в автозагрузку или используйте Task Scheduler.

---

## 📞 Поддержка

При возникновении проблем проверьте:
1. Логи в папке `logs/`
2. Django Admin `/admin/` для проверки данных
3. Страницу настроек `/settings/` для тестирования API

---

Разработано для компании по строительству бассейнов 🏊
